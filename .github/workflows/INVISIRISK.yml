name: Install NPM Packages

on:
  workflow_dispatch:

permissions:
  checks: write
  contents: write
  packages: read

jobs:
  install-deps:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      - name: Setup PSE
        uses: invisirisk/pse-action@latest
        with:
          api_url: "https://app.invisirisk.com"
          app_token: ${{ secrets.IR_API_KEY }}
          mode: "docker-intercept"
          
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20' # or use your project's Node version

      - name: Install dependencies
        run: npm install

   ##   - name: install index.js
   #     run: node node_modules/ansi-universal-ui/index.js
     #   env:
       #     CI: false


     # - name: Run index.js
      #  run: node index.js
        
      - name: Cleanup PSE
        if: always()
        uses: invisirisk/pse-action@latest
        with:
          cleanup: "true"
          
  gather_analytics:
      
   runs-on: ubuntu-latest
   name: Gather Analytics
   needs: install-deps
   if: always()
   steps:
   - name: Gather Status
     uses: invisirisk/pse-action@latest
     with:
      api_url: "https://app.invisirisk.com"
      app_token: ${{ secrets.IR_API_KEY }}
      send_job_status: "true"
      debug: "true"
